import { getThresholdAlert } from '../alerts'; import { alertFixtures } from '../__fixtures/alerts.fixtures'; describe('alerts', () => { it.each(alertFixtures)('$name', (f) => { const result = getThresholdAlert(f.previousPrice, f.currentPrice, f.threshold); if (f.expectedDirection) { expect(result).notToBe(null); expect(result).toContain(f.expectedDirection === 'raised' ? 'raised to' : 'dropped to'); } else { expect(result).toBeNull(); } }); });